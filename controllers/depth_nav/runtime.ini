; Tells Webots which Python interpreter to launch this controller with.
; This path is machine-specific (it points at the local `depthnav` conda env).
; Change COMMAND to your own env's python — see README "Setup".
[python]
COMMAND = /Volumes/Puneet-HDD/opt/anaconda3/envs/depthnav/bin/python
